# Phase P7 结果：旧路径清理与验收

- 删除 `public/js/modules`。
- 删除 contract 和 IPC 的 `domains/foundation/intelligence/interchange` 兼容目录。
- 目录清单升级到 P7，架构门禁验证新入口、薄 bootstrap、零旧根目录和运行闭包。
- 当前权威地图为 `frontend-interface-final-layout.md` 和 `dev/docs/PROJECT_STRUCTURE.md`。
- Obsidian 未混入本阶段，仍作为后续独立 Knowledge Source 工作。

最终门禁：全量测试 57/57、架构 9/9、真实 DeepSeek 单 turn、Windows unpacked 打包和打包产物 DSH E2E 均通过。
